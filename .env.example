# API Configuration
VITE_API_URL=/api

# Optional: slug to show at the root URL (e.g. sushi-spot → menu-o8l.pages.dev shows sushi-spot's menu)
# VITE_DEFAULT_SLUG=your-restaurant-slug

# D1 database is configured in wrangler.toml — no env var needed for database.
# To run scripts locally, use: npx tsx scripts/seed-admin.ts --local
