services:
  - type: web
    name: resturentmenu-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npx tsx server/index.ts
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
